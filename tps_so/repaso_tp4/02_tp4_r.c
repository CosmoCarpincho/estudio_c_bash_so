// Cargar vector y devolver los 10 elementos númericos ordenado de menor a mayor
#include <stdio.h>

#define nl() printf("\n") // new line
// o usar puts cuando solo es texto con salto de linea sin formato.

int array_num[10];
int size_array;

int ingresar_numero() { return cant_numeros; }

int main() {

        puts("Ingrese 10 números separados por espacio");
        for (int i = 0; i < ingresar_numero(); i++) {
                /* code */
        }

        int arreglo[10];
        for (int i = 0; i < 10; i++) {
                printf("Ingrese número: ");
                scanf("%i", arreglo[i]);
        }

        printf("Hello world");
        printf("Prueba");
        return 0;
}