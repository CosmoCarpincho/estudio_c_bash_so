#include <stdio.h>
int main(void)
{
    int a,b;
    if (a = (3 > 2 > 1))
    {
        printf("valor a : %i\n", a);
    }
    printf("valor a : %i\n", a);

    if (b = (1 < 2 < 3))
    {
        printf("valor b : %i\n", b);
    }
    printf("valor b : %i\n", b);
    
    return 0;
}
