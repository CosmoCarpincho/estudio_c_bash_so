#include <stdio.h>

int main (){
	
	printf("para ir a la definicion de una funcion apretar gd \n");
	printf("para ver la definicion en el archivo gf pero hay que tener todo guardado antes\ncuando se navega se crea un buffer y para viajar para atras en los buffers ctrl + o y para viajar hacia adelante en los buffer ctrl + i\n");

	//Un poco de texto para eliminar
	printf("para eliminar dw delete word elimina la palabra desde el principio\nundo u\nredo R\n");
	printf("eliminar desde la posicion del cursoar hasta el final de la linea es con d$ ...shift 4 => $ ");
int n1, n2;
int s = suma(n1, n2);

int suma(int n1, int n2){
	int resultado = n1 + n2;
return resultado;
}
	return 0;
}
