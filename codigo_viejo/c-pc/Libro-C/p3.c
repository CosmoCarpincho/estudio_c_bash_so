#include <stdio.h>

main()
{
    int c;

    // while ((c = getchar()) != EOF)
    //     putchar(c);

    // c = getchar();
    // while (c != EOF)
    // {
    //     putchar(c);
    //     c = getchar();
    // }

    // int a = getchar()!= EOF;
    //     putchar(a);
    int b = (getchar()!= EOF);
        putchar(b);
}       

