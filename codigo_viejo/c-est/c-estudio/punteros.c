/*  
  puntero es una variable que almacena una direccion de memoria

la variable dinamica no tiene nombre porque no se declara

la que se declara es el puntero y este la define porque tiene la direccion de memoria de la variable dinamica


Cuando se destruye una variable estatica.::: Cuando se termina de ejecutar el modulo que esta contenida. Por ejemplo si esta la variable dentro de un if o un for se elimina cuando sale del bloque
 *
 * Una lista es una sucesion de elementos denominados nodos, donde cada nodo almacena un valor y la direccion (puntero) del siguientenodo.
 */
/*
//Programa de tutorialesya 1
#include <stdio.h>
//#include <conio.h> no funciona en Linux, si en Windows

int main () {

int valor1=10;
int valor2=20;

int *pe;
pe=&valor1;

printf("Lo apuntado por pe es:%i\n", *pe);
printf("La direccion que almacena pe es:%p\n",pe);

pe=&valor2;
printf("Lo apuntado por pe es:%i\n", *pe);
printf("La direccion que almacena pe:%p\n",pe);
//getch();
return 0;
}
*/
